import styled from "styled-components";
import theme from "../theme";

export const StyledSpan = styled.span`
  height: 1rem;
  width: 1rem;
  border-radius: 50%;
  color: #fff;
  font-weight: bold;
  font-size: .8rem;
  `